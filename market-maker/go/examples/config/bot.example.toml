# Decibel market-maker — example TOML config (copy and fill in secrets).
# Priority: CLI flags > this file > environment variables > network preset defaults.

network = "testnet"
market_name = "BTC/USD"
spread = 0.001
order_size = 0.001
max_inventory = 0.005
skew_per_unit = 0.0001
max_margin_usage = 0.5
refresh_interval = 20.0
refresh_interval_jitter = 0.0 # 0 = fixed interval; else uniform ± jitter (s)
cooldown_s = 1.5
cancel_resync_s = 8.0
auto_flatten = false
flatten_aggression = 0.001
dry_run = true
auto_spread = false
spread_min = 0.0004
spread_max = 0.02
spread_no_fill_cycles = 3
spread_step = 0.0002

bearer_token = "YOUR_DECIBEL_BEARER_TOKEN"
subaccount_address = "0xYOUR_SUBACCOUNT_OBJECT_ADDRESS"
private_key = "0x1111111111111111111111111111111111111111111111111111111111111111"

# Optional overrides (uncomment as needed):
# node_api_key = ""
# package_address = ""
# aptos_fullnode_url = ""
# market_addr = ""
# rest_api_base = ""

# tg_bot_token = ""
# tg_admin_id = 0
# tg_alert_inventory = false
# tg_alert_inventory_interval_min = 30
# tg_strict_start = false
